# Bridge Server v2.0 - 优化后的核心依赖
# 从22个依赖精简到12个核心依赖

# Web框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# HTTP客户端
httpx>=0.25.0

# 数据验证和配置
pydantic>=2.0.0
pyyaml>=6.0

# 缓存系统
redis>=5.0.0
cachetools>=5.0.0

# 数据库ORM（可选，用于替代当前的SQLite直接操作）
sqlalchemy>=2.0.0

# 监控和指标
prometheus-client>=0.19.0

# 结构化日志（新增）
structlog>=23.0.0

# 重试机制（新增）
tenacity>=8.0.0

# 测试框架
pytest>=7.0.0
pytest-asyncio>=0.21.0

# 开发工具（可选）
black>=23.0.0
flake8>=6.0.0